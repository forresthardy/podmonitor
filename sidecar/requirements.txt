# Runtime dependencies for the transcription sidecar.
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
# CPU inference with int8 quantization; pulls in ctranslate2 and av (no system ffmpeg needed).
faster-whisper==1.1.1

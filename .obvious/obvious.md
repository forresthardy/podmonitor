# podmonitor

## Status: empty repository

This repository is currently **empty**. It contains no source code, no dependency
manifests, no Dockerfile/Compose/Makefile, and no documentation. Nothing has been
scaffolded and no framework or stack has been inferred.

## What this means for agents

- There is no application to build, run, test, or verify yet.
- Do not assume a language, runtime, package manager, ports, or commands.
- Do not scaffold an application on your own initiative; wait for real code.

## Filled in by the next setup run

Once actual code lands, the next onboarding run will populate:

- **Stack** — languages, frameworks, runtimes, package manager
- **Commands** — install, dev, build, test, lint
- **Codebase map** — folder-level overview
- **Local verification** — how to prove the dev stack is healthy

## Repository policy

- Default branch: `main`
- Merge method: merge commit (see `.obvious/config.yml`)
